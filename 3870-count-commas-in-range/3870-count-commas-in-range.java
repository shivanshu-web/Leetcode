class Solution {
    public int countCommas(int n) {
        if(n<1000){
            return 0;
        }
    //     String s = n +"";
    //    int nn = (s.charAt(0) - '0') * (int)Math.pow(10, s.length() - 1);
        return n-999;

        
    }
}